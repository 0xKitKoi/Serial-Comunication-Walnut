# GNU Make workspace makefile autogenerated by Premake

ifndef config
  config=debug
endif

ifndef verbose
  SILENT = @
endif

ifeq ($(config),debug)
  ImGui_config = debug
  GLFW_config = debug
  Walnut_config = debug
  WalnutApp_config = debug

else ifeq ($(config),release)
  ImGui_config = release
  GLFW_config = release
  Walnut_config = release
  WalnutApp_config = release

else ifeq ($(config),dist)
  ImGui_config = dist
  GLFW_config = dist
  Walnut_config = dist
  WalnutApp_config = dist

else
  $(error "invalid configuration $(config)")
endif

PROJECTS := ImGui GLFW Walnut WalnutApp

.PHONY: all clean help $(PROJECTS) Core Dependencies

all: $(PROJECTS)

Core: Walnut

Dependencies: GLFW ImGui

ImGui:
ifneq (,$(ImGui_config))
	@echo "==== Building ImGui ($(ImGui_config)) ===="
	@${MAKE} --no-print-directory -C Walnut/vendor/imgui -f Makefile config=$(ImGui_config)
endif

GLFW:
ifneq (,$(GLFW_config))
	@echo "==== Building GLFW ($(GLFW_config)) ===="
	@${MAKE} --no-print-directory -C Walnut/vendor/GLFW -f Makefile config=$(GLFW_config)
endif

Walnut: ImGui GLFW
ifneq (,$(Walnut_config))
	@echo "==== Building Walnut ($(Walnut_config)) ===="
	@${MAKE} --no-print-directory -C Walnut/Walnut -f Makefile config=$(Walnut_config)
endif

WalnutApp: Walnut ImGui GLFW
ifneq (,$(WalnutApp_config))
	@echo "==== Building WalnutApp ($(WalnutApp_config)) ===="
	@${MAKE} --no-print-directory -C WalnutApp -f Makefile config=$(WalnutApp_config)
endif

clean:
	@${MAKE} --no-print-directory -C Walnut/vendor/imgui -f Makefile clean
	@${MAKE} --no-print-directory -C Walnut/vendor/GLFW -f Makefile clean
	@${MAKE} --no-print-directory -C Walnut/Walnut -f Makefile clean
	@${MAKE} --no-print-directory -C WalnutApp -f Makefile clean

help:
	@echo "Usage: make [config=name] [target]"
	@echo ""
	@echo "CONFIGURATIONS:"
	@echo "  debug"
	@echo "  release"
	@echo "  dist"
	@echo ""
	@echo "TARGETS:"
	@echo "   all (default)"
	@echo "   clean"
	@echo "   ImGui"
	@echo "   GLFW"
	@echo "   Walnut"
	@echo "   WalnutApp"
	@echo ""
	@echo "For more information, see https://github.com/premake/premake-core/wiki"